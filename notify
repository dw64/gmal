#!/bin/bash

HOME=/home/xaos/gmal

cookie=$(fortune)

#$HOME/ttytter -keyf=$HOME/ttytter.key -status="$* | $(fortune)" >/dev/null 2>&1

echo "/dm dwrf64 " $(date +%H:%M) "$*" $(fortune|head -1) | $HOME/ttytter -keyf=$HOME/ttytter.key -script >/dev/null 2>&1