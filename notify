#!/bin/bash

HOME=/home/xaos/gmal

NAME=$(cat $HOME/name 2>/dev/null)
[ -z "$NAME" ] && NAME="rest"

cookie=$(fortune)

$HOME/ttytter -keyf=$HOME/ttytter.key -status="[$NAME] $* | $cookie" >/dev/null 2>&1
